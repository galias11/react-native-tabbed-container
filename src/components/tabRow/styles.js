// @Vendors
import { StyleSheet } from 'react-native';

// @Styles
import { rowHeight } from '../../styles/baseStyles';

const styles = StyleSheet.create({
  mainContainer: {
    height: rowHeight.small,
    width: '100%',
    flexDirection: 'row'
  }
});

export default styles;